services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: jira-clone
    env_file:
      - .env
    ports:
      - "3000:3000"
    volumes:
      - ./src/infrastructure/db:/app/src/infrastructure/db
    command: npm run start
